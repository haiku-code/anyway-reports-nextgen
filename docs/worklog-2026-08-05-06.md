# Worklog, 5 to 6 August 2026

Six commits on `ui/fixes`. Nothing merged to `main`.

## 2026-08-05

- `f0de32b` ynet logo now shows on mobile. Header swaps to a compact search
  once the in-page search scrolls away, driven by an IntersectionObserver.
- `a9d7cd5` School search redesigned. Split out `lib/searchStatus.ts` and
  `lib/viewport.ts`.
- `0f1b9f4` Hero cropped on mobile and its scrim rebuilt to pass WCAG AA.
  Fixed the header height oscillation with two thresholds. Image payload cut
  from 8,833KB to 524KB, `public/` from 24MB to 5.1MB.

## 2026-08-06

- `f276e95` Gender pie chart replaced with a split bar. Largest remainder
  rounding so the percentages always print to 100.
- `e76bf22` School stats render as one sentence. Sidebar widened to 36%.
- `4920ba5` Institution name printed above the map, beside a miniature of the
  green pin. The pin itself cannot be labelled: it lives in a cross origin
  iframe from anyway.co.il.

## Open

The green pin does not render for every reader. One Chrome screenshot showed
base tiles but no markers at all. Unreproduced: against headless Chromium it
draws at 1.0s across three institutions. An automated repro attempt was
invalid, the tab was backgrounded and rAF was throttled to zero.

If this turns out to be real, the caption from `4920ba5` should be held back
until the iframe fires `load`, since it claims a green pin that may not be on
screen.

Also open: `npm run lint` fails on two pre-existing errors in `Hero.tsx`.
